{
  "name": "@recon/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@recon/shared": "workspace:*",
    "decimal.js": "^10.4.3",
    "zod": "^3.23.8"
  },
  "comment:pendingDeps": "Add @uipath/uipath-typescript (^1.5.x) when wiring the SDK layer; the OAuth/REST client currently uses global fetch and needs no runtime dep.",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
