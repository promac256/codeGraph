{
  "name": "recon-suite",
  "version": "0.0.0",
  "private": true,
  "description": "UiPath-native financial reconciliation platform (TypeScript monorepo).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
